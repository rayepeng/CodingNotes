## MIPS学号排序代码

### 初始化

```MIPS
lui     $2, 0x5487  
ori     $2, $2, 0x3530 

# 将$2寄存器放置学号
sw      $2, 0($0) 
# 将数据存放到存储器中

addi    $11, $0, 8
#$11 = 8, 因为学号只有8位， 用来做循环计数

lw      $1, 0x0($0) 
#$1, $2 都存放了学号

add     $2, $0, $0 
# $2 清零

addi    $4, $0, 0x0f

#$4存放的值叫人看不懂

```

